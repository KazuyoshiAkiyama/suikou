# 具体的な版に固定する。stable のままにすると、将来の lint の追加で clippy が落ちて、
# コード以外の理由で CI が壊れる。実際に一度壊れている。
# 版を上げるときは、手元で clippy と test を通してから上げる。
# .github/workflows/ の dtolnay/rust-toolchain も同じ版に合わせる。
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
