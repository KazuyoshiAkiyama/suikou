# CLAUDE.md

このリポジトリで作業するときに、最初に読むものである。

## 何を作っているか

技術文書の lint とハーネスを作っている。日本語と英語を扱う。
生成AIが書いた技術文書の特徴を見つけて、そのうえで文書を保ちやすくするためのものである。

詳しい文書は `docs/` にある。Hugo で作れる形にしてあって、英語と日本語の両方を置いてある。
既定の言語は英語とする。作業の一覧は `TASKS.md` にある。

| 文書 | 内容 |
|---|---|
| `docs/content/*/design.md` | 設計の全体。測定した値と根拠を含む |
| `docs/content/*/metrics.md` | 指標の厳密な定義 |
| `docs/content/*/decisions.md` | 決めたことの経緯 |
| `docs/content/*/development.md` | 作り方と、遵守する事項 |

## 状態

test と fmt と clippy が、既定の feature でも `--features lindera-unidic` でも通る。
自己診断は実際の辞書に対して通っていて、素性の順序は確認してある。
ゴールデンテストは、M 系の規則と文書指標を参照の実装と照合ている。

| 部分 | 状態 |
|---|---|
| ブロックの抽出と前処理 | 書いてある。単体テストあり |
| M1 から M7 の規則 | 書いてある。単体テストあり |
| 日本語と英語の文書指標 | 書いてある |
| 出力の形と Markdown の整形 | 書いてある |
| lindera の接続 | 書いてある。lindera 6.0.0 で確認した |
| `check`、`brief`、`baseline`、`terms`、`selftest` | 書いてある |
| ゴールデンテスト | M 系と文書指標をつないである |
| `mcp` | 書いてある。依存を足さず JSON-RPC を自分で書いた |
| `daemon` | 置かない。起動が数ミリ秒で完了すると測定したため |
| textlint の規則 | 書いてある。ゴールデンの件数が Rust と一致する |
| 配布用のワークフロー | 3つの対象で確認した。タグを付与するまで配られない |

## 作りとテスト

```sh
cargo test --all                           # 形態素解析なし。速い。まずこれ
cargo test --all --features lindera-unidic # 辞書を含む。遅い
cargo fmt --all
cargo clippy --all-targets -- -D warnings
```

既定の feature に lindera を入れていない。
そのため辞書を落とさずに本体を確認できる。

## 遵守する事項

遵守する事項の詳細は `docs/content/ja/development.md` にある。
ここには要点だけを書く。

### 直感で規則を足さない

**これがこのリポジトリでいちばん重い制約である。**

文体の特徴について直感で立てた仮説は、確認すると大半は成立しない。
このプロジェクトでも、設計の途中で立てた仮説の多くが測定した値で落ちている。

規則を追加するときは、実際に測定した値か、明文のスタイルガイドの規約を示す。
`corpus/baselines.toml` の `[rejected]` に載っている指標を導入してはならない。

### 前処理の順序を変えない

`markdown.rs` の `preprocess` は順序に意味がある。
バックスラッシュのエスケープを戻す処理を後ろへ動かすと、文分割が成り立たない。
取り除いた範囲は同じ数の改行に置き換える。
そうしないと、指摘の行番号が使う人の開いているファイルと一致しなくなる。

### 指標の対象範囲を混ぜない

語彙の指標は `Document::body()` を使い、文の構成の指標は `Document::prose()` を使う。
語彙の指標を地の文だけで測定してはならない。
出力の大半が箇条書きになるモデルがあり、その場合に本文の8割以上を破棄することになる。

### 形態素解析を直に呼ばない

lindera への依存は `tokenizer.rs` の `LinderaMorphology` に閉じ込めてある。
ほかのモジュールは `Morphology` トレイト越しに使う。
指標を追加するときも、この境を越えない。

### 版を推測で書かない

依存は `=` で固定してある。`Cargo.lock` も置く。
更新するときは crates.io で実在を確認してから書く。
lindera を更新した場合は `verify_schema` を必ず通す。
素性の順序が一致しなくなると、語種と活用形が気付かれずに壊れる。

### トークンの出る量を増やさない

ルールの説明はルールにつき一度だけ出力する。
ひとつのルールにつき示す位置は `Report::MAX_POSITIONS_PER_RULE` 件までとする。
閾値の内に収まっている指標は出力しない。
文書指標は数ではなく指示の文に変換して出力する。

## 文書を書くときの約束

このリポジトリの文書は、このツールが課す規則に自分で従って書く。

- 箇条書きは完全な文で導いて、導入文が項目と文法で結びつく形にしない。
- 地の文に項目の数を書かない。
- 見出しに連番を付けない。
- 時点に依存する語を使わない。
- 暗喩を使わない。必要なら直喩か例え話にする。
- プロの技術文書で使われない和語を繰り返さない。`register/rare-wago` が検出する。

置く前に `suikou check` を適用することにする。
条件は error と warning が出ないことまでとする。info は参考として読む。

指標を最小化することを目標にしてはならない。
漢語率は info であって参考値である。
それをゼロにしようとして和語の動詞に置き換えた結果、文体が劣化した経緯が D-27 にある。
漢語率を下げるなら、定着したカタカナ語を使い、漢語の名詞を重ねないようにする。

## 作業の記録

判断が分かれた箇所は `docs/content/*/decisions.md` に追記する。
後から読む人が、同じ検討を繰り返さずに済むようにするためである。

書いている途中で確信のない細かい値や見送った案が出た場合は、
成果のコードやコメントではなく、決定の記録か `TASKS.md` に書く。
