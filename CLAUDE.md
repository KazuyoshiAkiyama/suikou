# CLAUDE.md

このリポジトリで作業するときに最初に読む。

## 何を作っているか

技術文書の lint とハーネス。日本語と英語に対応する。
生成AIが書いた技術文書の特徴を検出し、同時に文書の保守性を高める。

詳しい設計は `docs/DESIGN.md`、指標の定義は `docs/METRICS.md`、
決定の経緯は `docs/DECISIONS.md` にある。作業の一覧は `TASKS.md`。

## 状態

test、fmt、clippy が既定の feature でも `--features lindera-unidic` でも通る。
`verify_schema` は実辞書に対して通っており、素性の並びは確認済みである。
ゴールデンテストは M 系と文書指標を参照実装と突き合わせている。

| 部分 | 状態 |
|---|---|
| ブロック抽出と前処理 | 実装済み。単体テストあり |
| M1 から M7 の規則 | 実装済み。単体テストあり |
| 日本語と英語の文書指標 | 実装済み |
| 出力スキーマと Markdown 整形 | 実装済み |
| lindera の接続 | 実装済み。lindera 6.0.0 で検証済み |
| ゴールデンテスト | M 系と文書指標は接続済み。`unescape.json` は未接続 |
| CLI の各サブコマンド | 未実装。`todo!` が置いてある |
| textlint の局所ルール | 未実装 |
| リリースのワークフロー | 3ターゲットで検証済み。タグを打つまで公開されない |

## ビルドとテスト

```sh
cargo test --all                          # 形態素解析なし。速い。まずこれ
cargo test --all --features lindera-unidic # 辞書を含む。遅い
cargo fmt --all
cargo clippy --all-targets -- -D warnings
```

既定の feature に lindera を含めていない。
辞書を落とさずに本体のロジックを検証できるようにしてある。

## 守ること

### 直感で規則を足さない

**これがこのリポジトリで最も重要な制約である。**

文体の特徴について直感で立てた仮説は、実証すると大半が外れる。
このプロジェクトでも、設計の途中で立てた仮説の多くが実測で否定された。
翻訳調の研究でも同じことが起きている。

規則を足すときは、次のいずれかの根拠を示す。

- 人間コーパスとAI出力の実測値。範囲が重ならないこと
- 明文のスタイルガイドの規定。出典を示すこと

`corpus/baselines.toml` の `[rejected]` に、棄却した指標と理由が書いてある。
**ここに載っている指標を再導入してはならない。**
文長の変動係数、外来語率、英語の現在分詞節と名詞化がこれにあたる。
いずれも「AIらしさの指標」として広く言われているが、実測では人間と重なる。

### 前処理の順序を変えない

`markdown.rs` の `preprocess` は順序に意味がある。
バックスラッシュのエスケープ解除を後ろに動かすと文分割が成立しない。
AWS の doc_source がこの形式であり、解除しない場合の平均文長は3倍以上に膨らむ。

`tests/golden/input/en_escaped.md` がこの挙動を固定している。

### 指標の対象範囲を混ぜない

- 語彙系（MATTR、漢字率、漢語率、指示詞、形式名詞）は `Document::body()`
- 文構造系（文長、読点、連用中止、テ形接続）は `Document::prose()`

語彙系を地の文だけで計算してはならない。
出力の大半が箇条書きであるモデルがあり、その場合に本文の8割以上を捨てることになる。

### 形態素解析を直接呼ばない

lindera への依存は `tokenizer.rs` の `LinderaMorphology` に閉じ込めてある。
ほかのモジュールは `Morphology` トレイト越しに使う。

新しい指標を足すときも、この境界を越えない。
越えると、辞書なしでテストが回らなくなる。

### バージョンを推測で書かない

依存は `=` で厳密に固定してある。`Cargo.lock` もコミットする。
更新するときは crates.io で実在を確認してから書く。

lindera を更新した場合は `LinderaMorphology::verify_schema` を必ず通す。
素性の並びがずれると語種と活用形が静かに壊れ、
漢語率と連用中止の値が誤ったまま動く。

### トークンの出力量を増やさない

レポートの形式には制約がある。詳細は `docs/DESIGN.md` の出力スキーマの節。

- ルールの説明文はルールにつき一度だけ出す。行ごとに繰り返さない
- 一つのルールにつき提示する位置は `Report::MAX_POSITIONS_PER_RULE` 件まで
- 閾値の範囲内にある指標は出力しない
- 文書指標は数値ではなく指示文に変換して出す

この制約は、層ごとに lint と修正を往復させないための設計である。
形式を変えるときは、この目的を壊していないかを確かめる。

## 文書を書くときの約束

このリポジトリの文書は、suikou が課す規則に自分で従って書いてある。

- 箇条書きは完全な文で導入する。導入文が項目と文法的に繋がる形にしない
- 地の文に項目数を書かない。「3つある」ではなく「以下のとおり」と書く
- 見出しに連番を付けない
- 時点に依存する語を使わない。「現在」「最新の」「今後」など
- 隠喩を使わない。比喩が要る場合は直喩か例え話にする

自分の道具の規則に従えない文書を置くと、道具の説得力がなくなる。

## 作業の記録

判断が分かれた箇所は `docs/DECISIONS.md` に追記する。
後から読む人が同じ検討を繰り返さずに済むようにする。

実装の途中で確信のない具体値や却下した案が出たら、
成果物のコードやコメントではなく、この文書か `TASKS.md` に書く。
