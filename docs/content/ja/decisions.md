---
title: "決めたことの記録"
weight: 30
---

後から読む人が、同じ検討を繰り返さずに済むようにするための記録である。
判断が分かれた箇所を、そのつど書き足していく。

## D-01 文書の全体で測る指標を textlint に収めない

textlint はノードの位置に紐づけて指摘する仕組みである。
文書の全体でしか測れない指標には、紐づけるべき位置がない。
さらに、その指標の多くは足りないことの指摘である。
形式名詞が少ない、指示詞が少ない、直喩の標識が少ない。
足りないものには、どこにも位置がない。

はじめは Document ノードに対して報告する textlint の規則として書く案を採った。
これは誤りだった。
textlint の中に収めると解析の土台が JavaScript に縛られる。
そうなると語種を返す形態素解析が得られず、漢語率を書けなくなる。
書く都合で、確かめを通った指標を捨てることになって、順序が逆である。

前面の道具が textlint を呼び出して、自前の文書の解析とまとめる形を採る。
MCP は前面の道具そのものが出せばよい。
むしろ、まとめたレポートを一度に返せる分、エージェントには都合がよい。

## D-02 書く言語は Rust を中心として、Python は較正と探索に絞る

確かめを通った指標は、すべて純 Rust で書ける。
lindera-unidic が UniDic のすべての素性を返して、語種と活用形が取れるためである。

Python が要ったのは分詞節、名詞化、修飾の積み重ね、係り受けの距離である。
これらはすべて、測った値で落とした指標である。
深い構文の解析が要る指標ほど実験の組み立てに敏感で、先行研究の効き目が移らなかった。

そこで層を分ける。
決まった指標は Rust で書いて、探索と較正は Python で行う。
Python で使えると分かった指標だけを Rust に移す。

Claude Code の hooks から Write と Edit のたびに動かす使われ方では、起動の費用が効いてくる。
spaCy の読み込みに2秒から5秒かかるため、Python を本番の経路に置くことはできない。

## D-03 Markdown のパーサを使わず行で処理する

`research/` の参照の実装が行で処理していて、
ゴールデンテストの値をそろえるために同じやり方を採る。
依存が減る分、版の違いによる事故も減る。

## D-04 形態素解析をトレイトの裏に隔離する

lindera の API と UniDic の素性の並びは、版で変わる。
版に寄りかかるコードをひとつの構造体に閉じ込めれば、壊れうる面が数十行に縮む。

加えて、既定の feature に lindera を入れないことで、
辞書を落とさずに規則と指標を確かめられる。そのため変更の確かめが速くなる。

## D-05 起動のときに素性の並びを自分で確かめる

版を固めるだけでは、更新したときに黙って壊れる問題が残る。
素性の並びがずれると、語種と活用形が別の位置から読まれる。
値は返るが、その中身が違う。

漢語率と連用中止がこの二つに寄りかかっているため、
壊れた値のまま動くと、指摘の中身そのものが誤りになる。
知っている語を流して確かめて、ずれていれば落ちる方がよい。

## D-06 文体で閾値を分けない

常体と敬体で指標が変わるかを、二つのモデルの出力で比べた。
モデルの間で向きがそろわず、決まった効き目は見られなかった。
その差はモデルの差に吸い取られる。

文体の判定は残すが、その用途は体言止めのような文体に寄りかかる規則を当てるかどうかに絞る。
閾値の表を二重に持つ必要はない。

## D-07 形式名詞と漢語率の severity を info に置く

文書の種類による振れが大きい。
手順書では形式名詞が閾値に触れて、概念の解説では漢語率が人間ネイティブの値に近づく。
漢語率は話題への感度も高い。

日本語の人間のコーパスは解説書に偏っていて、手順書の型が足りない。
種類ごとの値がそろうまでは info に置いて、そろった時点で warning に上げる。

## D-08 先行研究の効き目を持ち込まない

英語の分詞節について、人間の5.3倍という報告をもとに閾値を9から10と見積もった。
測ってみると人間の幅と重なって、使えなかった。名詞化についても同じである。

理由は実験の組み立て方の違いと見ている。
報告の元は人間が書いた文章を与えて同じ文体で続けさせる形だが、こちらは文書を作らせている。

先行研究は見立てのもとにはなるが、その効き目を仕事をまたいで持ち込んではならない。

## D-09 保ちやすさの層は人間の平均に合わせない

箇条書きの導入文を正しく書いているのは AWS S3 ユーザーガイドだけだった。
Linux kernel は不完全な導入の方が多い。
人間の平均に合わせると、この層は意味を失う。

この層は AI らしさを消す層ではない。
人間が書いた文書の大半より良くするための層である。

## D-10 予防で消せる層と lint でしか消せない層を分ける

書式を抑える指示を与えた条件と与えない条件を比べた。
書式は完全に消えたが、語彙と文の組み立ては変わらなかった。

保ちやすさの指示についても同じ実験を行った。
組み立ての規則は短い指示で完全に守られ、
時点に寄りかかる語は詳しい指示で初めてゼロになった。
項目の数、見出しの連番、手で振った番号は、指示がなくても起きない。

したがって `brief` には、指示で守れることだけを入れる。
効かない縛りを入れることは、トークンの無駄であって、直しすぎる危うさもある。

副次の効き目として、保ちやすさの指示だけで文書指標も良くなった。
導入文を書かせたことで地の文が生まれて、地の文比率と箇条書き割合が人間の幅に近づいた。

## D-11 語の辞書を同梱せず文書から作る

生きたたとえと死んだたとえの見分けが、たとえの層でいちばん難しい。
cache や handshake は分野で定まった語であって、たとえではない。

指摘されて有名になった語は、出る数が減る。
2024年の初めに指摘された語の群は、その直後から減った。同梱の辞書は古びる。

自分の文書から作る辞書は古びない。
プロジェクトごとに語が違うという理由に加えて、
これが辞書の古びへの構えになる。

## D-12 漢語率のテストは期待値の側を直した

`kango_ratio_counts_only_content_words` が落ちていた。
入力「設定を変更すること」に対して実装は 0.5 を返して、テストは 0.5 超を見込んでいた。

`TASKS.md` はテストが落ちたら実装を直すと定めている。この件は例外にあたる。
テストの注記は自立語を「設定」「変更」「こと」の3語と数えていたが、
`FakeMorphology` の仕様には「する」が動詞として入っていて、自立語は4語になる。
指標の定めは、分母を自立語のうち語種が 和 漢 外 混 のものとしている。
そのため動詞の「する」も分母に入って、実装が返す 2/4 が定めどおりの値である。
実装をテストに合わせると、定めから外れる。

テストのねらいは、助詞を分母から外すことの確かめにある。
期待値を 0.5 のぴたり一致に変えても、そのねらいは保たれる。
助詞「を」を分母に入れた場合の値は 0.4 であって、両者は見分けられる。

実装を直してよいのは、実装が定めから外れている場合に限る。
期待値が定めから外れている場合は、期待値を直す。

## D-13 M2 と M6 は出る数ごとに数える

参照の実装は1行あたりの出る数を数えるが、
Rust の実装はブロックの単位で高々1件しか指摘していなかった。
`tests/golden/input/en_maintainability.md` の
「The validator currently reports ... The latest release」が1行に2語を含んでいて、
参照の実装の 2 に対して Rust が 1 を返していた。

出る数ごとに数える側を採った。根拠はこのプロジェクト自身の縛りにある。
1行に2件あって1件しか報告しないと、
指摘された側だけを直した文書が `check --quiet` でまた落ちる。
層ごとに lint と直しを往復させないという設計が、そこで崩れる。

`Position` に使われていない `column` があったのは、
もともと出る数ごとに位置を持たせる見込みだったことを示している。
出る数ごとに列を持たせたことで、同じ行の複数の違反を見分けられる。

M7 は行末に錨を打つため1行に高々1件であって、この変更の影響を受けない。

## D-14 語数と文数は相対の誤差で突き合わせる

突き合わせの方針は、比率に絶対の誤差 0.02、密度に 0.5、M 系にぴたり一致を定めていた。
語数はこのいずれにも当てはまらない。

`en_prose.md` の語数は参照の実装が 445、Rust が 444 だった。
理由は `trade-off` の扱いにある。
spaCy は三つのトークンに割って記号を除くため2語と数えて、空白での分割は1語と数える。
この文書に含まれるハイフンの語はちょうど一つであって、差と一致する。

トークナイザの定めの違いであって、実装の誤りではない。
ぴたり一致を求めると、定めが違うものを同じ数にしろと求めることになる。
相対の誤差 1% で突き合わせて、決まったずれだけを捕らえる。

## D-15 形式名詞の対象範囲が参照の実装と定めで食い違っている

指標の定めは、語彙の指標を地の文とリスト項目の本文で測るとしていて、
形式名詞をその一つに挙げている。Rust の実装はこれに従っている。

参照の実装 `research/jametrics.py` は、指示詞について
地の文だけの値と、地の文とリスト項目の本文の値の両方を出す。
その一方で形式名詞は、地の文だけの値しか出さない。

`tests/golden/input/ja_maintainability.md` で測ると、
地の文だけなら 14.98、リスト項目の本文を含めると 15.77 になる。
その差は 0.79 であって、密度の許す幅 0.5 を超える。

ここで実装を変えてはならない。定めには理由が書いてある。
出力の大半が箇条書きになるモデルがあって、
地の文だけで測ると本文の8割以上を捨てることになる。

食い違うのは閾値の側である。
人間の幅 11.095 から 11.163 と D5 の閾値 8.5 は、地の文だけの定めで測った値である。
リスト項目を含めると値が上がるため、D5 は較正のときより出にくくなる。
D5 の severity を info に置く判断（D-07）は、この点でも正しかった。

較正をやり直すまで、この食い違いは開いたままになる。
やり直しには `research/jametrics.py` に地の文とリスト項目の値を足して、
コーパスに対して測り直すことが要る。

## D-16 辞書を埋め込んだひとつのバイナリで配る

配る形を決めるにあたって、埋め込んだ辞書の大きさを測った。

| 中身 | 大きさ |
|---|---|
| 辞書を同梱したバイナリ | 191 MB |
| 同じものを xz で圧縮 | 26 MB |
| 辞書を含まないバイナリ | 1.1 MB |

`strip` は効かない。中身がシンボルではなく辞書のデータであるためである。

辞書を外に出して初回に取ってくる形も採れる。
lindera 6.0.0 には `load_dictionary_from_path` があって、複製せずに読める。
それでも埋め込みを採った。その理由は使われ方にある。
hooks から Write と Edit のたびに動かす道具では、失敗の経路の数がそのまま信頼になる。
外に出した辞書は、欠け、壊れ、版の食い違いという経路を増やす。
版の食い違いは自己診断が捕らえるが、捕らえたところで使う人は動けない。
取ってきたあとにネットワークが要らない形の方が、この使われ方には合う。

配る単位は OS ごとの書庫とする。
すべての OS をひとつに束ねると圧縮後 78 MB になって、
使う人は使わない OS の分を必ず落とすことになる。

対象は x86_64 の Linux (musl)、Apple Silicon の macOS、x86_64 の Windows に絞った。
Linux を musl にするのは、glibc の版に寄りかからせないためである。
古いディストロやコンテナで動かない道具は、hooks から呼ぶ使われ方では痛い。
Intel Mac と ARM Linux は外した。要る場合は、使う人がソースから作る。

## D-17 辞書の feature を既定で有効にしない

`suikou-cli` に `lindera-unidic` を足したが、既定では切ったままにした。
既定に入れると `cargo test --all` が辞書を引き込んで、
辞書なしで本体を確かめられるという前提（D-04）が壊れる。

代わりに二つの手当てをした。
配る用のビルドは feature を書いて作って、起動して辞書のあるなしを確かめてから梱包する。
辞書なしの版で日本語を求められた場合は、`morphology::load` がその場で落ちる。

黙って空の解析結果を返してはならない。
形態素解析が空を返すと、漢語率も連用中止も形式名詞も 0 になる。
0 は閾値の下にあるため、指摘が出るものと出ないものが入り混じる。
そして「指摘なし」という誤った結びに至る経路がある。

どちらの版かは `suikou --version` で見分けられるようにした。
使う人が握っているバイナリを特定できないと、日本語が解析されない理由をたどれない。

## D-18 配る物の確かめは辞書の実体に対して行う

配る用のワークフローを `act` で走らせて見つかった。
梱包されたバイナリは 1.2 MB しかないのに、`--version` は「UniDic 同梱」と出していた。
辞書は 191 MB あるため、両立しない。

理由はリンカが使われないコードを落とすことにある。
サブコマンドがすべて `todo!()` であったため、`morphology::load` に届く経路がなく、
埋め込んだ辞書のデータごと落とされていた。
届く経路を作って作り直すと 191.5 MB になって、実際に解析の結果を返した。

問題はスモークテストの側にもあった。
`--version` の表示は `#[cfg]` による組み立てのときの分岐であって、
feature が有効かどうかしか語らない。辞書が実在するかは語らない。
辞書を持たないバイナリに対して、この確かめは通ってしまう。

`selftest` サブコマンドを足して、実際に辞書を読んで解析させる形に変えた。
配る物の確かめは、組み立ての設定ではなく、できあがった物の振る舞いに対して行う。
自己診断が素性の並びを実際の辞書で確かめているのと同じ考え方である。

副次の効き目として、`morphology::load` に届く経路ができたため、
`crates/suikou-cli/src/morphology.rs` に置いていた抑制を外せた。
使う人にとっても、握っているバイナリが思ったとおりのものかを確かめる手だてになる。

## D-19 指摘の行番号は前処理の前の行に合わせる

自分の文書に `suikou check` をかけて見つかった。
指摘された行番号が、実際の行より4つ前を指していた。
その差は、取り除いたフロントマターの行数とぴたり一致する。

前処理はフロントマターとコードブロックを中身ごと消していた。
そのため後続の行が前へ詰まって、行番号が実際のファイルとずれる。

取り除いた範囲を、同じ数の改行に置き換える形に変えた。
空の行は数えないため、空でない行の数と箇条書き割合は変わらない。

lint の指摘は、使う人が開いているファイルの行を指さなければ使えない。
値が正しくても、位置がずれていればその指摘は役に立たない。

## D-20 文書を Hugo のサイトにして、既定の言語を英語にする

文書を `docs/` の直下に置く形から、`docs/content/{en,ja}/` に移した。
既定の言語は英語とする。

テーマを外から取らず、自前の最小のレイアウトを置く。
依存を増やさないためであって、版を固めるという本体の方針を文書の側でも保つためでもある。

文書の正本をひとつに定めたことで、サイトとリポジトリで同じ中身を二重に持たずに済む。

## D-21 brief の詳しさはプロファイルではなくフラグに持たせる

設計の文書には、brief の詳しさをプロファイルの設定として持たせる案がある。
今回の実装では、この案を採らずに `--detail concise|balanced|detailed` という
フラグだけで制御する形にした。

理由を挙げる。Profile は指標の閾値を保つための構造体である。
そこへ brief 専用の設定を足すと、この構造体が二つの役目を抱えることになる。
較正で書き戻す値と、出力の形だけを選ぶ値を同じ構造体に混ぜると、
`suikou baseline` が書き戻すべき範囲があいまいになる。閾値だけを保つという
Profile の役目を保つほうを選んだ。

フラグの水準も、当初の書き付けから変えた。`TASKS.md` の最初の案は
concise と detailed の二つだけを挙げていたが、実装の途中で balanced を
間に置くほうが自然だと分かった。組み立ての規則を1文で書きつつ時点依存語だけを
すべて列挙するという brief の縛りは、balanced という真ん中の水準にそのまま収まる。
concise はそこからさらに切り詰めた形にし、detailed は規則ごとに理由まで添えた。

プロファイルへ持たせる案は、決めていないことの一覧に残す。
用途ごとに既定の detail を変えたいという求めが実際に出たときに、
プロファイルへ移すかどうかを決めることにする。

## D-22 baseline の閾値は Tukey の外れ値境界で決める

`suikou baseline` を実装するにあたって、閾値の決め方を選ぶ必要があった。
`docs/content/ja/design.md` は、閾値を人間の幅の上と AI の幅の下との間に置くと定めている。
ただし baseline が見るのは人間コーパスだけであり、AI 側の分布は分からない。
そこで、人間の分布そのものから見て外れ値と言える境界を出す方針にした。

採ったのは Tukey の外れ値境界である。第1四分位と第3四分位の差を四分位範囲と呼び、
その1.5倍を四分位の外側へ足した点を境界とする。1.5という係数は Tukey (1977) の
「外れ値」の定義に使われている値であり、統計で広く使われている。
恣意的な余裕を独自に決めるより、名前の付いた定義に乗る方が根拠を示しやすいと判じた。
3.0倍の「極端な外れ値」という、より緩い定義もあるが、サンプル数の少ない使われ方では
境界が外側へ寄りすぎて較正の意味が薄れるため、採らなかった。

四分位を割る方法は hinge 法にした。サンプルを中央値で二つに割り、それぞれの中央値を
第1四分位と第3四分位とする。サンプル数が奇数の場合、中央値そのものはどちらの半分にも
含めない。線形補間を使うパーセンタイル法もあるが、hinge 法は仕組みが単純で、
テストの期待値を手計算で確かめやすい。

サンプルが4件に満たない指標は較正しないことにした。3件以下だと、上半分か下半分の
どちらかが1件になり、四分位範囲が分布の広がりではなく個々の値でほぼ決まってしまう。
較正できない指標を0や仮の値で埋めて出すと、使う人が根拠のない閾値に気づけない。
そのため出力からその指標を外し、理由を標準エラーに出す形にした。

この一次実装には欠陥があった。below 方向の指標は、比率や千字あたりの出現数のように
値域が0以上に限られる。サンプルが一かたまりに寄っていると、Q1 - 1.5*IQR が0を下回る。
この道具自身の日本語文書（`docs/content/ja/*.md`、`README.ja.md`、`CLAUDE.md`、
`TASKS.md`）をサンプルにして較正したところ、`ja.demonstrative_per_1k` の境界が -4.96、
`ja.keishiki_meishi_per_1k` の境界が -4.98 になって出た。密度と比率は負にならないため、
この境界を下回る実測値は原理的に存在せず、`check::evaluate` の below の判定
（value < threshold）を満たす文書は現れない。有効な TOML として読み戻せてしまうため、
この閾値を含むプロファイルへ `suikou check` をかけても指摘ゼロが返り、指摘ゼロだけを
見る検証はこの欠陥を較正の成功と取り違えた。

0でクランプする案は採らなかった。境界を0にしても、密度と比率はもともと0以上であり、
value < 0 を満たす実測値はやはり存在しないため、閾値が発火しないことに変わりがない。
代わりに、below の境界が0以下まで下がった場合を較正の失敗として扱うことにした。
サンプル不足の場合と同じく、その指標は出力から外し、理由を標準エラーに出す。

above 方向には同じ手当てをしていない。below の下限（0）はどの指標にも共通するが、
above の上限は指標によって違う。比率は1が上限になる一方、千字あたりの出現数には
決まった上限がない。指標ごとの上限を較正のロジックに持ち込むと、その指標固有の
知識を混ぜ込むことになるため、below だけに絞った。

`guidance` と `direction` と `severity` は、同梱の oss プロファイルからそのまま
引き継ぐことにした。baseline を走らせるたびにこの文面を書き写すと、oss.toml を
直したときに baseline の出力だけ古いまま取り残される経路ができる。
較正の対象は value だけに絞った。

`corpus/fetch.sh` で取れるコーパスは手元になく、ライセンスの都合でリポジトリに
含めていない。そのため、この決め方で実際に `corpus/baselines.toml` や
同梱のプロファイルと同じ桁の値が出るかどうかは確かめていない。
`tests/golden/input/` の文書は数が少なく、指標ごとのサンプルが4件に届かないため、
その代わりの確かめもできない。コーパスを手元に置ける環境で実際に走らせて
突き合わせるまで、TASKS.md に未検証として残す。

較正できた below 方向の閾値が実際に働くことは確かめてある。上の文書をサンプルにして
生き残った `ja.prose_ratio`（境界 0.69）を含むプロファイルを作り、箇条書きばかりの
文書へ `suikou check --profile` をかけると発火し、地の文だけの文書では発火しないことを
実行結果で確かめた。

## D-23 textlint プリセットの実装で分かれた判断

`packages/textlint-rule-preset-tech-maintainability`（T7）を書く過程で、
一つにまとめきれない判断が複数出た。以下に並べる。

**textlint が実際に表示するルールIDは、標準の設定では `maint/` にならない。**
指示は Rust側と揃えて `maint/list-lead-in` のような ID にすることを求めていた。
`@textlint/config-loader` の `loader.js` と `@textlint/utils` の
`normalizeTextlintRulePresetKey` を読んで確かめると、プリセットの接頭辞は
`.textlintrc` に書いた設定キーから、先頭の `preset-` を取り除いた文字列になる。
このパッケージを標準の書き方どおり `"preset-tech-maintainability": true` で
読み込むと、表示される ID は `tech-maintainability/list-lead-in` になる。
設定キーは `preset-` で始まらないとプリセットとして認識されず
（`isPresetRuleKey` がそう定めている）、認識された場合はその `preset-` が
必ず剥がされるため、標準の読み込み経路では `maint/` という接頭辞に
どうやっても届かない。

個々のルールの名前（`list-lead-in` のような接頭辞なしの部分）は指示どおり
Rust側の名前と揃えてある。接頭辞だけは textlint 自身の命名規則に従うほかなく、
それを曲げるとしたら npm パッケージ名を `textlint-rule-preset-maint` のように
変える必要があるが、パッケージの置き場所である
`packages/textlint-rule-preset-tech-maintainability` という名前と食い違って
かえって分かりにくくなる。パッケージ名は置き場所に合わせたままとし、
実際に表示される ID が `tech-maintainability/` 接頭辞になることを README に
明記した。ゴールデンテストの突き合わせでは、`@textlint/kernel` を直に使い
`maint/` 接頭辞で手動登録しているため、この制約の影響を受けない
（`test/support/lint.js`）。

**AST を経由せず行ベースで判定する。** textlint は Markdown を段落や見出しなどのノードに分けて渡す。ソフトラップで複数行に
またがる段落は、textlint の中でひとつの Paragraph ノードにまとまる。一方 `markdown.rs`
の `Document::parse` は行ごとにブロックを作り、ソフトラップの続きも独立した行として扱う。
この食い違いは、`en_maintainability.md` の14〜15行目のような、ひとつの文が2行にまたがる
段落で表面化する。ノード単位で判定すると2行分の文字列がひとつにまとまり、Rust側が
決して結び付けない語同士が正規表現の一致範囲に入ってしまう。

とりうる道は二つあった。textlint の Paragraph ノードのテキストをそのまま判定にかける道と、
`Document::parse` と同じ行ベースの手順を JS に移植して、textlint の側では文字列としての
原文しか使わない道である。前者は「textlint らしい」実装ではあるが、ノードの粒度が変わる
たびに件数がずれる。後者を採った。`src/lib/blocks.js` が `markdown.rs` の
`preprocess`・`strip_inline`・`Document::parse`・`list_blocks`・`block_before` を、
正規表現も含めてそのまま移植している。各ルールは `context.getSource(node)` で原文を取り、
自前のブロック抽出にかけてから判定する。textlint が担うのは、ファイルの読み込みと
設定、報告の一本化であって、構造の解析ではない。

この選び方の代償は、報告する行・列が前処理後の文字列に基づく点である。原文の生の
文字列上での位置は、エスケープの解除やインライン記法の除去によって前処理後の位置と
食い違うことがある。`src/lib/position.js` は、一致した文字列を対象の行の中で順に
探して原文の文字インデックスへ近似的に結びつける。この近似は指摘を見失わせない
ためのものであり、正確さそのものを保証しない。`tests/golden/` との突き合わせは
件数だけを見ており、位置の正確さは条件に含めていない。

**IPADIC は UniDic の体言4分類をひとつの品詞にまとめる。**
M1・M5の日本語判定は、行末や項目末尾のトークンが体言止めかどうかを見る。
suikou-core は UniDic を使い、品詞大分類が名詞・接尾辞・代名詞・形状詞のいずれかを
体言とみなす。textlint 界隈の標準である kuromojin は IPADIC を使い、この4分類を
持たない。

kuromojin 3.0.1 に実測させると、UniDic で接尾辞・代名詞・形状詞に立つ語は、IPADIC
では名詞の下位分類（品詞細分類1が代名詞・接尾・形容動詞語幹）として現れる。
「これ」は名詞/代名詞、「委員長」の「長」は名詞/接尾、「静か」は名詞/形容動詞語幹
だった。したがって IPADIC では、pos が名詞であることのひとつの条件に見分けをまとめて
よい。`src/lib/morphology-ja.js` の `isTaigen` はこの実測に基づく。

連用形の判定も同様に実測で確かめた。UniDic は「連用形-一般」のように活用形を細分
するが、IPADIC は単に「連用形」という値を返す（「使い」「し」で確かめた）。
`cform.startsWith("連用形")` という Rust 側の判定はそのまま移植して問題ない。

**説明文の言語は文書の言語に合わせる。** Rust側は日本語で固定している。
suikou-core の `finding()` は、文書が英語であっても日本語の説明文を返す。
`en_maintainability.md` に対する `suikou check` の出力で確かめた。

textlint プリセットの利用者には英語文書だけを書く人もいる。日本語しか読めない
説明文を出すと、その人たちにとって指摘が使えない。判定ロジックと正規表現は
Rust側と一致させる一方、説明文だけは検出した言語に合わせて日本語・英語を
出し分けることにした。`src/lib/messages.js` に両方を持たせてある。件数の
突き合わせに説明文の中身は関わらないため、この差はゴールデンテストの完了条件に
影響しない。

**テストは textlint-tester ではなく node:test を直接使う。**
textlint-tester は Mocha の `describe`/`it` を前提に作られている。それらが
グローバルに無い環境では、`method.apply(this)` を呼ぶだけの代用に落ちる。
この代用は `testValid`・`testInvalid` が返す Promise を待たないため、
`node --test` の実行環境にそのまま載せると、lint が終わる前にテストが
成功したことになってしまう箇所があった。

Mocha を依存に加えて textlint-tester を使う道と、`@textlint/kernel` を
直に使う薄い層を書く道の二つを検討した。依存を増やさない方を選び、
`test/support/lint.js` に `TextlintKernel` を包む数行を置いた。プリセットの
全ルールを `maint/` 接頭辞付きで登録し、`async/await` で素直に結果を待つ。
`node --test` の非同期テストとそのまま噛み合う。

**`@textlint-ja/textlint-rule-preset-ai-writing` の `no-ai-colon-continuation`
と重なる指摘を、あえて抑えない。** そのルールは、コロンの手前が述語で終わる
文を kuromojin で検出しており、見分けの作りが M1 の日本語版とよく似ている。
目的は違う。あちらは AI が書いた文体の癖を検出し、こちらは項目の増減に
弱い箇条書きの構造を検出する。それでも同じ行が両方から指摘されることがある。
どちらか一方を優先させる仕組みは設けなかった。二つのプリセットを同時に
有効にする利用者は、両方を読んだうえで自分の文書に合う方を選べる立場に
あり、片方を黙らせると選ぶ材料を減らすことになる。README にこの重なりを
明記した。

## D-24 terms の取り出しは品詞だけでなく構造で語を切る

日本語は、名詞が連続する範囲を複合語ひとつとしてまとめる。
UniDic は「素」や「性」のような接尾辞を pos1 が接尾辞になる形で切り出すため、
名詞だけを対象にすると「形態素解析」が「形態」と「解析」に割れ、
「保守性」が「保守」だけになった。この崩れは、自分の文書にこの道具をかけて見つけた。

接尾辞を名詞と同じ扱いに加えると、今度は「つ」のような助数詞まで
複合語の先頭に立ってしまう。UniDic の素性からは助数詞だけを見分けられる
粒度の情報を持っていないため、助数詞の一覧を新たに作ることはしない。
代わりに、接尾辞は直前に名詞の連なりが既にある場合に限って連なりを伸ばす形にした。
数詞で連なりが断たれたあとの接尾辞は、単独では語にならない。

UniDic は空白を語の境切りとしてだけ使い、トークンとしては残さない。
半角の語が隣り合うトークンをそのままつなぐと、本文にない語ができる。
自分の文書にある「Linux kernel」（`design.md` の複数箇所にある）で、
「Linuxkernel」という本文にない語ができることを確かめた。
半角英数字の語どうしが隣り合うときは間に空白を1個入れる。
半角の語と日本語の語が隣り合うとき（「API仕様」のような語）は空白を入れない。
日本語の文中でこの組み合わせが直に続く場合、元の文でも空白を置かない書き方が普通であるため。

一般語を除く一覧は `metrics/ja.rs` の `KEISHIKI_MEISHI` をそのまま使い、
別の一覧を作り直すことはしない。
一般語が語彙集に混じっても、たとえの判定から外れるだけで害は小さい。
分野の語を取りこぼす害の方が大きいと見て、こぼさない側へ倒した。

自分の文書にかけると、1字の候補が上位を占めた。
395語のうち54語が1字で、上位20語は文(50)、形(49)、語(48)、指示(43)、値(36)と続き、
地(28)、数(27)を経て、版(22)、行(22)、層(20)まで並んだ。
「地」は「地の文」が「の」で割れた残骸で、残りは日本語の一般語として
どんな文章にも高頻度で出る、分野に紐付かない語である。

個別の語を挙げて除く一覧は作らない。挙げる基準そのものが思いつきになるためである。
代わりに、結果の文字数という構造だけで絞り、1字の候補を対象から外した。
日本語の術語取り出しでは1字の候補を除く扱いが広く採られている。
意味の運び手になるのはほとんどが複数字の複合語であり、1字の語は最小の形態素で
多義になりやすく、単独では分野を特定できないためである。
「文書」のような2字の1トークン語は、この絞り込みの対象にならない。
絞り込んだあとの自分の文書での上位語は、指示、規則、指標、人間、文書、日本語、
指摘、閾値、辞書、実装のように変わり、1字の語は残らなかった。

英語には形態素解析器も文の切り分けもない。
頭文字だけの語、文の先頭ではない大文字始まりの語、識別子ひとつの形をした
インラインコードを手がかりにする。表の行はこの対象から外した。
表の列の値は文の形をしておらず文頭の手掛かりが働かないうえ、
同じ値が行をまたいで繰り返され出る数の下限もすり抜けることを、
自分の文書にかけて確かめた。

見出しの先頭に ID が立つ形にも同じ問題があった。
`decisions.md` 自身の見出し「D-04 The tokenizer hides behind a trait」で、
先頭の語だけを文頭とみなすと、次に来る The を文中の語と見誤った。
先頭から ID 風の語が続く間は、まだ文頭の続きとみなす形に直した。

見出しの先頭に ID 風の語が並ぶ場合には、別の崩れが残っている。
英語版の `decisions.md` の見出し「D-13 M2 and M6 count every occurrence」で、
`D-13` と `M2` の間に区切りの記号がないため、隣り合う語がひとつの複合語
「D-13 M2」としてまとまる。`M2` という語だけの出る数がここで失われ、
出る数の下限を割ることがある。取りこぼしであって、本文にない語を
作る誤りではないため、害は小さいと見て今回は直さない。
直すには、隣り合う ID 風の語をどこまでつなぎしてよいかの判断が要る。
「AWS S3」のように隣り合う頭字語をつなぎして初めて意味をなす固有名詞もあり、
それを壊さずに済む規則をまだ見つけていない。

出る数の下限は既定で2とする。
1回しか出ない語は、分野の語とたまたまの言い回しを見分ける材料がない。
terms のもう一つの目的である語の一貫性の確認も、同じ語が繰り返し出て
初めて成り立つ。この値は実測ではなく、この二つの狙いから導いた下限である。

この取り出しの結果は、たとえの層が使うためのものである。
そのたとえの層はまだ書いていないため、取り出しのよしあしを最後に決めるのは、
その層を書いたときのことになる。
